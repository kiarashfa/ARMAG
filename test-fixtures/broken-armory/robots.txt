# A deliberately wrong robots.txt — the well-meaning mistake SPEC.md §12 exists
# to prevent. Blocking a noindex page means the page is never fetched, so the
# noindex is never read, and it can sit in the index as "fetched but not
# indexed" indefinitely.
User-agent: *
Disallow: /ARMAG/armory/
